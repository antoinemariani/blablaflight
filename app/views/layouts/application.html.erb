<!DOCTYPE html>
<html>
  <head>
    <title>Blablaflight</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <%= render "layouts/navbar" %>
    <%# <p class="notice"><%= notice %>
    <%# <p class="alert"><%= alert %>

    <% if notice %>
    <div class="flash flash-success alert alert-dismissible fade show" role="notice">
      <span><strong>Yay!</strong> 🎉 you successfully signed in to BlaBlaFlight. 🛩</span>
      <a data-bs-dismiss="notice" aria-label="Close">
        <i class="fas fa-times"></i>
      </a>
    </div>
    <% end %>

    <%= yield %>
  </body>
</html>
